spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/proiect_tap
    username: postgres
    password: 1q2w3e
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB